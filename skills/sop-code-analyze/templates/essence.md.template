# 项目精华：{{PROJECT_NAME}}（ESSENCE）

> 由 sop-code-analyze **Phase E 精华提取** 生成。本文件是**可迁移资产**——从本项目提炼出"值得在新项目 / 同类项目中模仿复用"的优秀设计、模式与逻辑，以及可规避的坑。
> 提炼日期：{{DATE}} | 分析对象：{{REPO_PATH}} | 依据：Phase A-D 产出 + 源码（分析报告见 `reports/analyze/md/analyze_report.md`）

## 0. 精华总览（TL;DR）

| # | 精华点 | 类别 | 价值 | 出处 |
|---|--------|------|------|------|
| 1 | {{ESSENCE_TITLE}} | 设计/模式/逻辑 | ⭐核心 / ◎亮点 / ·细节 | `<文件>:<行>` |
| 2 | {{ESSENCE_TITLE}} | 设计/模式/逻辑 | ⭐核心 / ◎亮点 / ·细节 | `<文件>:<行>` |

> **类别**：设计=架构层面取舍 | 模式=实现层可复用套路 | 逻辑=算法/边界细节
> **价值**：⭐核心（可直接整体模仿的决策）| ◎亮点（局部值得借鉴）| ·细节（小而美）

## 1. 优秀设计（架构层）— 值得整体模仿的取舍

### D1. {{DESIGN_TITLE}}
- **是什么**：{{DESCRIPTION}}
- **为什么好**：{{RATIONALE — 解决了什么问题，权衡了什么}}
- **证据**：`{{FILE}}:{{LINE}}`
- **可复用场景**：{{REUSE_CONDITIONS — 何时在新项目用，何时不该用}}

### D2. {{DESIGN_TITLE}}
- ...

## 2. 优秀模式（实现层）— 可复用的套路

### P1. {{PATTERN_TITLE}}
- **是什么**：{{DESCRIPTION}}
- **为什么好**：{{RATIONALE}}
- **证据**：`{{FILE}}:{{LINE}}`
- **可复用场景**：{{REUSE_CONDITIONS}}

### P2. {{PATTERN_TITLE}}
- ...

## 3. 精妙逻辑（细节层）— 小而精的算法与边界处理

### L1. {{LOGIC_TITLE}}
- **是什么**：{{DESCRIPTION}}
- **妙在哪**：{{WHY_CLEVER}}
- **证据**：`{{FILE}}:{{LINE}}`

### L2. {{LOGIC_TITLE}}
- ...

## 4. 可规避的坑（反模式）— 从 hidden_couplings 提炼的教训

### A1. {{PITFALL_TITLE}}
- **问题**：{{PROBLEM}}
- **影响**：{{IMPACT}}
- **正确做法**：{{HOW_TO_AVOID}}

### A2. {{PITFALL_TITLE}}
- ...

---

**提炼者自检**：
- [ ] 每条精华都有 `file:line` 证据；推断内容标 `[inferred]`
- [ ] 无流水账式的"这个文件是干这个的"
- [ ] 每条能回答"好在哪 / 何时复用"，答不上已删除
- [ ] 第 4 节与 `analyze_report` 的 `hidden_couplings` / 需人工复核项对得上
- [ ] 篇幅与项目复杂度匹配（大项目 15-30 条，中小项目 5-15 条）
